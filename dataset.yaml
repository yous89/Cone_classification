# dataset.yaml — Minimal für YOLOv8 (Detection)
path:  "gib path zu Datensatz ein"                           
train: dataset_split/train/images      # zugehörige Labels müssen in dataset_split/train/labels liegen
val:   dataset_split/val/images
test:  dataset_split/test/images       # optional, aber praktisch

nc: 4
names: ["blue_cone", "yellow_cone", "orange_cone", "large_orange_cone"]
